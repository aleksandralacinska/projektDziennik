<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DziennikMVVM"
        tools:targetApi="31">

        <activity
            android:name=".view.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".view.HomeActivity" android:exported="true"></activity>
        <activity android:name=".view.JournalActivity" android:exported="true"></activity>
        <activity android:name=".view.CalendarActivity" android:exported="true"></activity>
        <activity android:name=".view.AddEntryActivity" android:exported="true"></activity>
        <activity android:name=".view.EditEntryActivity" android:exported="true"></activity>
    </application>

</manifest>
